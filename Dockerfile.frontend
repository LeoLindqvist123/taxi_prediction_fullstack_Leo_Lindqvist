FROM python:3.12-slim

WORKDIR /app

COPY src/ ./src/

RUN pip install fastapi uvicorn pandas numpy scikit-learn joblib

EXPOSE 8501

CMD ["uvicorn", "src.taxipred.frontend.app:app", "--host", "0.0.0.0", "--port", "8501"]